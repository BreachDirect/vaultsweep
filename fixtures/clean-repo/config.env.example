# Clean fixture — no real secrets

STELLAR_SECRET=${STELLAR_SECRET_KEY}

# Load from environment — see .env.example
ANTHROPIC_API_KEY=
ZAP_API_KEY=

# Example placeholder only
HORIZON_URL=https://horizon-testnet.stellar.org
